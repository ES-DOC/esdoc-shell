{
    "cell_type": "markdown",
    "metadata": {},
    "source": [
        "# {{ pc_idx + 1 }}. {{ pc.names(2) }}  \n",
        "*{{ pc.description }}*"
    ]
},
{% for p_idx, p in enumerate(pc.properties) %}
    {% include model-realm-property.tornado %}
{% end %}